package com.dev.fordevs.model;
// TODO: explain why u use enums in comment (user shouldnt be able to add spec)
// TODO: maybe use hashmap instead of enums and block access for normal user?
public enum TaskSpecialization {
    BACKEND,
    FRONTEND,
    ML,
    DEVOPS,
    UIUX // UI/UX
}
